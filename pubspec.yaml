name: custom_tab_view
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.11.0-93.1.beta
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  mix: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
